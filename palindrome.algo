PROCEDURE Is_palindrome(s,l,r)
VAR
    l,r:INTEGER;
BEGIN
    IF (length(s)<2) THEN
        write(s is palindrome)
    END_IF
    l=0;
    r=length(s)-1;
    IF (s[left]=s[right]) THEN
    
        Is_palindrome(s,l+1,r-1)
    ELSE
        write(s is not palindrome)
    END_IF
END